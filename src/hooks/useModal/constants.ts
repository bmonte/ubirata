import { Drawer } from '@/components/Drawer';
import { PopulationDrawer } from '@/components/PopulationDrawer';

export const DEFAULT_MODALS = {
  DRAWER_MODAL: Drawer,
  POPULATION_DRAWER: PopulationDrawer,
};
